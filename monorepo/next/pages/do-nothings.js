import Head from 'next/head'
import Image from 'next/image'
import { Inter } from 'next/font/google'
import styles from '@/styles/Home.module.css'

const inter = Inter({ subsets: ['latin'] })
const STIO_DOMAIN = process.env.STIO_DOMAIN || 'shaneburkhart.com'
const STIO_USER = process.env.STIO_USER || 'shane';

const LINKS = [
  { label: "Dozzle (Docker Logs)", href: `http://dozzle.${STIO_USER}.${STIO_DOMAIN}` },
]

export default function Home() {
  return (
    <>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="max-w-xl mx-auto mt-6">
				<div className="flex">
					<div className="flex">
						{LINKS.map(({ label, href }) => (
							<a
								key={label}
								href={href}
								target="_blank"
								className="block p-4 mb-4 text-2xl font-bold text-center text-white bg-blue-500 rounded-md"
							>{label}</a>
						))}
					</div>
					<div className="flex">
						{LINKS.map(({ label, href }) => (
							<a
								key={label}
								href={href}
								target="_blank"
								className="block p-4 mb-4 text-2xl font-bold text-center text-white bg-blue-500 rounded-md"
							>{label}</a>
						))}
					</div>
				</div>
      </main>
    </>
  )
}

